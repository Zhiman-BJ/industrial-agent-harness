---
name: chip-layout-inspect
description: Inspect GDS or OAS layout cells, layers, and geometry.
---

Inspect the selected layout with KLayout rendering APIs. Identify the cell, visible layers, and viewport for each observation. A rendered layout does not establish DRC or LVS success; retain the artifact hash and use dedicated verification for those claims.
